ingressRoute:
  dashboard:
    enabled: true
    matchRule: Host(`dashboard.loclahost`)
    entryPoints:
      - web
providers:
  kubernetesGateway:
    enabled: true
gateway:
  listeners:
    web:
      namespacePolicy:
        from: All
